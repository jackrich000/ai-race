# AI Benchmark Tracker - Task Board

> **Owner**: Jack | **Agent**: Claude Code
> Statuses: `[ ]` Todo | `[-]` In Progress | `[x]` Done

---

## Backlog

- [ ] **Pipeline fixes: report-never-posts bug + alias drift + OpenAI/xAI streak — planned + red-teamed 2026-05-29, ready to execute** — Full self-contained execution plan in `PIPELINE_FIXES_PLAN.md` at repo root. Three issues found while investigating why the 2026-05-29 scheduled run posted no GitHub issue (the run succeeded; only the report failed). (1) **Label bug**: `run-pipeline.mjs` adds labels `pipeline-alert`/`pipeline-failure` that don't exist in the repo, so `gh issue create` fails hard and the whole report is lost — first triggered 2026-05-29 when the streak alert label first fired. Fix = create the labels (check-then-create) + harden `postGitHubIssue()` so a bad label can never again eat the report. (2) **Alias drift**: `BENCHMARK_ALIASES` in `extraction.js` is a second source of truth that drifted from `config.js`; OSWorld-Verified + MMMU-Pro have no alias so their model-card scores (e.g. Opus 4.8 OSWorld 83.4) get classified untracked and stranded in `benchmark_raw`. Fix = config-derived aliases (kill the drift class), migrate existing 10 benchmarks 1:1 behind a regression test, then add OSWorld/MMMU-Pro (auto-ingest), keep Terminal-Bench excluded. Plus a gated `scripts/retriage-raw.mjs` backfill to recover stranded rows. (3) **OpenAI/xAI streak** (no scores yielded since 2026-05-07): diagnose only — first check if it's just the alias gap (could collapse into #2) before assuming a scraper break. Decisions locked with Jack: single bundled PR for label+alias; auto-ingest OSWorld/MMMU-Pro; diagnose #3 before fixing. Next session: read `PIPELINE_FIXES_PLAN.md` and start PR 1.
- [x] **Fix automated extraction reliability — DeepSeek + 3 bonus Chinese labs** — Replaced the broken DeepSeek HF DOM scanner with a pure-HTTP HF Hub API path (PR #46, merged 2026-05-07). Same path covers DeepSeek (`deepseek-ai`), Kimi (`moonshotai`), MiniMax (`MiniMaxAI`), Zhipu (`zai-org`); Qwen retains its existing `qwen.ai` card scanner. First production run (issue #47) ingested DeepSeek-V4-Pro/Flash with per-mode variants (Max/High/Non-Think), Kimi-K2.6, GLM-5.1; GLM-5.1 became the new HLE best for Chinese Leaders at 52.3. ByteDance/Doubao remains the only Chinese sub-lab not automated (no flagship on HF as open weights — punt unless they start publishing).
- [x] **Pipeline health checks** (PR #48, merged 2026-05-07) — Three complementary defenses:
  - **Source-level abort thresholds** in `scripts/update-data.js`. AA: 100, SWE: 30, ARC: 50, Epoch: 100. If a fetcher returns below threshold, ingestion aborts before any DB writes; existing `benchmark_scores` is preserved. Pipeline issue gets a "Source Health Failure" section. Healthy runs render a "Source Health" table in the issue body for ongoing trend visibility.
  - **Scraped-article streak alert** via new `pipeline_runs` table. Tracks `articles_scraped` and `scores_yielded` per lab per run. Alerts when 4 consecutive runs show either no_articles (scraper broken) or no_scores (extraction prompt / page template drift). Won't fire until 4 historical runs exist (~4 weeks).
  - **Migration runner** (`scripts/apply-migrations.mjs`, `npm run migrate`). Applies `migrations/*.sql` via the Supabase Management API using a Personal Access Token. SHA-256 hash tracking detects content drift; transactions wrap each migration for atomic rollback. Stays manual for now (chose option A — automating in CI deferred until migrations become more frequent). Known limitation: hash check is line-ending sensitive; if `npm run migrate` is ever run from a non-Windows checkout, it'll spuriously claim all migrations have been edited. Fix when this becomes relevant.
- [x] **Consider new benchmarks to add to the site, now GPQA Diamond and AIME are saturated** (PR #50, merged 2026-05-12) — Added 2 of the 3 evaluated candidates:
  - **ARC-AGI-3**: shipped. Reasoning frontier, currently unsolved (all labs <0.6%). Wired via the existing ARC Prize fetcher; ARC v3 leaderboard moved to `<lab>-<model>` modelId naming so `ARC_LAB_PATTERNS` in `lib/pipeline.js` was extended to match the new format. LLM analysis prompt got a "sub-5% = don't claim leadership" safeguard.
  - **OSWorld-Verified**: shipped. Computer use, new capability category. Verified history from Epoch CSV (`os_world_external.csv`) + unverified model card scores already in `benchmark_raw`. Treats the original OSWorld and the July 2025 Verified relaunch as one timeline; pre-Q3 2025 tooltip points carry an "Original OSWorld" era tag.
  - **GDPval**: deferred to the **non-% scoring** backlog item below. Data investigation: AA's panel publishes only Elo (no wins+ties %), OpenAI's panel publishes wins+ties %, no clean way to merge on a 0-100% axis. Will revisit when chart gets non-% support.
  - **Terminal-Bench 2.0 / BrowseComp / MMMU-Pro**: evaluated and rejected — overlapping with SWE-bench Pro, model-card-only data, near-saturation respectively. Notes in PR #50 discussion.
- [ ] **Non-percentage scoring benchmarks** — METR Time Horizons (minutes), Codeforces / LiveCodeBench Pro (Elo), **GDPval-AA Elo**. Requires a different chart type or normalization approach. GDPval has full lab coverage on AA's Elo leaderboard (368+ models) but the chart's fixed 0-100% Y-axis blocks ingestion. When this lands, GDPval-AA Elo is the easiest add — scrape from the AA evaluations page (it's there in the SSR'd HTML chunks). See PR #50 discussion for the panel-vs-Elo methodology findings.
- [ ] **Rethink aggregate view on Lab Race tab — ideation paused 2026-05-12, plan locked** — Ideation plan in `LAB_RACE_PLAN.md` at repo root. Headline = one race ("% of frontier" composite per lab over time); drill-down = many races (per-capability detail). Bucket-first aggregation on the 8-capability taxonomy. Margin preserved (no tally), coverage gaps shown visually (no imputation), active-only cohort. Shares data/lifecycle/rollup spine with Pace of Change but diverges on cohort rule. Four open items + four six-month regret flags + drill-down format choice to resolve before plan mode. Next session: `/clear` and read `LAB_RACE_PLAN.md`.
- [ ] **Merge Pace of Progress into Frontier page as a sparkline — discussion 2026-05-15, direction agreed, plan not yet locked** — Sanity check sparked it: the Frontier callout's "67.4% median increase across 6 active benchmarks (last 12 months)" doesn't reconcile with the Pace tab's numbers, and a user toggling between the two tabs would reasonably expect them to tell the same story. Agreed direction: replace the Pace tab with a narrow sparkline below the Frontier chart on the Frontier page; single unified analysis section interprets both level (Frontier) and rate (Pace) together. Considered four indicator shapes (sparkline-only, sparkline + capability chips, capability bar strip, stat tiles only); picked **sparkline-only**.

  **Decisions made**:
  - Drop the Pace of Progress tab entirely. Sparkline becomes the only pace surface on the site.
  - Drop Terminal-Bench 2.0 from `PACE_COHORT` (16 → 15) so the sparkline measures exactly the benchmarks shown on the Frontier chart above it. Removes the only cohort difference between the two views (Terminal-Bench was the sole `chartModes: ["pace"]` benchmark).
  - Terminal-Bench 2.0 fate: **pause tracking** (option 2 of 3 evaluated). Stop extracting; leave existing rows + the `BENCHMARK_META` row in place so it's easy to revive if a future Coding capability split makes room for it.
  - Per-capability breakdown (currently the Pace bar chart) becomes **LLM-mediated only** — capability numbers still computed and fed to the analysis prompt, but not visualised on the page. Trade accepted with eyes open (see regret flag below).
  - Cohort principle: Pace sparkline cohort == Frontier chart cohort (15 benchmarks, with saturated/deprecated rendered as grey dashed lines on Frontier).
  - The four axes that differed between the Frontier callout and Pace today (cohort / metric unit / aggregation / time framing) collapse to Pace's logic as the source of truth, except where the Frontier callout's relative-% framing is more user-legible (TBD per stat tile decision below).

  **Open questions to resolve before plan mode**:
  - **Analysis section restructure**: Frontier card absorbs pace stats + LLM narrative (tentative rec), OR new unified "Progress" card replaces Frontier card, OR keep Frontier card lean and add a separate Pace card (likely too dense — four cards total).
  - **Stat tile economy**: Replace the existing "median % increase" tile with "avg pp/quarter" (tentative rec — avoids the "why don't these numbers match" problem that started the discussion), OR keep both (acknowledges they answer different questions). Possibly add a "top capability by pace" tile to surface *some* capability signal visually without the bar chart.
  - **Sparkline visual specifics**: Y-axis baseline (zero vs auto-fit); X-axis alignment with the Frontier chart's range; current-quarter annotation (dashed line, matching Pace tab convention); width matching the Frontier chart container; tooltip content — specifically whether to relocate the current Pace tooltip's "top mover per capability per quarter" detail here, since that's where the capability signal lives now.
  - **Mobile**: how the sparkline renders at <=640px; whether it stays useful, gets hidden, or collapses to a stat-tile fallback.
  - **Race + Cost callouts**: stay as-is structurally, but the cohort question still applies. Race today uses the strict-active filter via `isInChartMode("race")`; do we widen to match the new Pace == Frontier cohort principle, or leave Race using its own cohort logic?

  **Cross-cutting work when this lands**:
  - `PACE_COHORT` in `lib/pace-chart.js`: drop `terminal-bench-2-0`.
  - `tests/pace-chart.test.js`: update cohort expectations.
  - Terminal-Bench 2.0: stop scraping (`lib/lab-sources.js` likely); leave existing `benchmark_scores` rows in place; consider whether to also strip its `chartModes` field or leave it as `["pace"]` for revival ease.
  - LLM prompt in `lib/analysis-prompt.js`: update to incorporate pace data; instruct on cohort alignment, on how level vs rate relate, and on capability commentary expectations.
  - Regenerate all cached analyses (`node scripts/generate-analyses.js`, every preset) after the prompt + callout structure changes. The existing LLM commentary references the old callout numbers and will be incoherent until regenerated.
  - Page IA: remove Pace tab from navigation; route any direct hash links back to the merged Frontier page.

  **Six-month regret flags**:
  - **Capability breakdown becomes LLM-mediated only**, and the LLM has a documented misranking habit (per CLAUDE.md note on ARC-AGI-2 leadership). If "where is progress happening" turns out to be the most-cited signal post-launch, hiding it in prose will feel like a regression. Mitigations queued: preserve top-mover detail in the sparkline tooltip; add a "top capability by pace" stat tile.
  - **Pace might want its own surface again later** (confidence bands, log scale, smoothing, capability-level lines). Merging now might be uncoupled later. Trade accepted for IA simplicity now.
  - **Terminal-Bench 2.0 stops contributing to the pace number** — the only active Coding-successor with verified Epoch data. Coding is still represented by SWE-bench Pro (active) and four saturated Coding benchmarks (historical contribution), so Coding isn't unrepresented, just narrower.

  **Sequencing note**: this task touches the same surfaces as the "Sharpen the AI-generated analysis for non-technical / executive audiences" sub-bullet under "Make the site work for non-technical audiences" below. Both involve rewriting the LLM prompt and regenerating cached analyses — worth sequencing together (same PR, or a tight series) so we don't burn the cache twice.
- [-] **Make the site work for non-technical audiences**:
  - [x] **Capability labels shipped to Frontier Progress chart** (PR #TBD, 2026-05-13): 6-capability taxonomy (Coding, Math, Expert Reasoning, Visual Reasoning, Computer Use, Novel Problem Solving) anchors the legend with capability-first columns + colour-per-capability + "N defeated" multi-tooltip. Companion changes: `category` → `capability` rename in `BENCHMARK_META`; MMMU-Pro added as Visual Reasoning anchor with new AA page-scrape fetcher; ARC-AGI-2 deprecated (replaced by ARC-AGI-3); per-benchmark `BENCHMARK_COLORS` const deleted. Race + Cost modes intentionally left flat. Full rationale in `project_capability_taxonomy.md`.
  - [x] **Pace of Progress chart shipped** (PRs #53 + #54, 2026-05-13): new 4th tab. Headline line = flat mean of raw quarterly frontier deltas across a 16-benchmark cohort, Q4 2024+, current quarter dashed. Underneath: per-capability average pace over the last 12 months, with N=1 capabilities (Visual Reasoning, Computer Use) rendered outline-only. Methodology revised away from the original capability-weighted approach after red team showed it amplified N=1 buckets 2.7× and broke the headline's unit. Cohort gating via new `chartModes` field on `BENCHMARK_META`: Terminal-Bench 2.0 is Pace-only; MMLU-Pro and Aider Polyglot appear on Frontier (as saturated history) and Pace, not Race. Pre-existing latent bug also caught and fixed: `data-loader.js` had no pagination and was silently truncating client-side at Supabase's 1000-row cap (we just crossed it). Out of scope for v1: LLM commentary for Pace, custom export canvas, sensitivity ribbons. Mobile polish items noted in self-review (bar-label crowding at 375px, mode-toggle wrap to 2 rows).
  - Better structure expanded benchmark descriptions in methodology section: clearly separate a) what the benchmark is from b) where we get the data (deferred — capability-labels PR did a light label-only update; full restructure still pending)
  - Sharpen the AI-generated analysis for non-technical / executive audiences. Manually extract the best insights from the data, then work backwards to tune the analysis prompt. What would you highlight presenting to a C-level audience? (e.g. not "ARC-AGI 2 scores increased" but "scores on the hardest benchmark for testing problem-solving capabilities increased..."). Jack flagged 2026-05-13 that this should be a wider rethink now that the chart layout has changed — pace mode currently has no commentary and frontier/race callouts may reference benchmarks differently post-`chartModes`.
- [ ] **Small UI fixes**:
  - Add "last updated" date to chart so visitors know how current the data is
  - ~~Deprecated benchmark icon renders in front of (rather than behind) the tooltip on the chart~~ — fixed in PR #50 (inactivity marker plugin moved from `afterDraw` to per-dataset `afterDatasetDraw`)
  - Pre-relaunch QA pass: mobile experience, broken links, chart load on slow connections, OG preview rendering when shared on LinkedIn / X / Reddit / Hacker News
- [ ] **Set targets for engagement, to justify continued work after re-launch** — Pre-commit to numeric success criteria *before* promoting, so post-launch evaluation isn't post-hoc rationalisation. Set at least one target per dimension below — having both a reach target and an inbound target lets you distinguish "post didn't travel" from "site didn't convert". For each target, define a **time horizon** (30/60/90 days) and a **kill criterion** (if missed, stop or pivot — not just success thresholds).
  - **Reach**: unique visitors / page views (top of funnel — did anyone see it?)
  - **Engagement quality**: time on site, returning visitors (did the content actually land?)
  - **Social signal**: LinkedIn engagement, Hacker News points, Reddit upvotes, Substack subscribers (did it travel?)
  - **Inbound interest**: contacts / inquiries / mentions (did it create opportunity? — most decision-relevant for "is this worth continuing?")
- [ ] **Share best insights from the site on LinkedIn / Reddit / Substack / Hacker News** — Identify the most compelling data stories and package them for social sharing. Sequence after the accessibility + executive-tuning work so the site is ready for non-technical traffic.
- [ ] **Quarterly maintenance: ground truth re-verification + API credit top-up — Next: 2026-07-01** — Manually re-verify each documented ground truth in `project_extraction_groundtruths.md` (Anthropic 16, OpenAI 16, xAI 5, Google 15, DeepSeek 14, Qwen 8) against the current source page. Also: at the next checkpoint, manually verify and document GTs for the four HF API labs (DeepSeek-V4-Pro, Kimi-K2.6, MiniMax-M2.7, Zhipu/GLM-5.1) — they currently have `scripts/validate-extraction.mjs` smoke checks derived from a single production run but aren't yet in the manual reference.
  1. Open each `URL` in a browser
  2. Confirm every listed score still appears with the same benchmark name and value
  3. If a page has been re-templated or a score has changed, update both `scripts/validate-extraction.mjs` ground truths AND `project_extraction_groundtruths.md`
  4. Re-run `node scripts/validate-extraction.mjs` to confirm the suite passes against the updated GTs
  5. Check Anthropic API credit balance + Browserbase usage; top up if low (no auto-recharge configured). Pipeline health checks would catch a hard outage between checkpoints, but quarterly proactive top-up avoids any gap.
  6. After completion, update this task date to 2026-10-01

  Why: Lab pages can be silently re-templated with new layouts. The fresh-article canary catches new-article failures but not re-templated old articles, so the validation suite can drift from reality without anyone noticing.
- [ ] **Modernise frontend stack** — Migrate to TypeScript + build step (Vite), then adopt a component framework (Vue or Svelte). Addresses known architectural debt: monolithic app.js, manual DOM manipulation, no type safety. Do in two phases: (1) TypeScript + Vite, (2) framework. Requires external research + subagent red team per new process.
- [ ] **Differentiated branding / visual identity** — Come up with branding that could extend across apps, blog, slides. Opportunity to define a reusable visual identity. Explore testing the [frontend-design skill](https://github.com/anthropics/skills/blob/main/skills/frontend-design/SKILL.md) for this.
